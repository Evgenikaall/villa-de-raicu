# Start from a lightweight base image
FROM alpine

# Optional: set working directory
WORKDIR /app

# Optional: copy files (add as needed)
# COPY . .

# Optional: install packages (uncomment if needed)
# RUN apk add --no-cache curl bash

# Default command (does nothing)
CMD ["echo", "Hello from empty Dockerfile!"]
